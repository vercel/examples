# Flags SDK Secret
# Generate with: node -e "console.log(crypto.randomBytes(32).toString('base64url'))"
FLAGS_SECRET=

# Vercel Flags SDK Key (automatically set when using Vercel Flags)
# This is pulled automatically via `vercel env pull`
FLAGS=
