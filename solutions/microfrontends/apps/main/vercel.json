{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter main build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter main... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}