# Express API Starter

Simple Express server with JSON endpoints and logging.

Endpoints:

- GET /health
- GET /time
- GET /todos
- POST /todos { title }
- DELETE /todos/:id

Run locally:

```bash
node server.js
```
