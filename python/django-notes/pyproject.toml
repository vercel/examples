[project]
name = "django-notes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0",
    "psycopg[binary]>=3.1",
]

[tool.vercel.scripts]
build = 'if [ -n "$DATABASE_URL" ]; then python manage.py migrate --noinput; fi'
