export declare const ExceptionEndpoint = "https://statsigapi.net/v1/sdk_exception";
declare type ExtraDataExtractor = () => Promise<Record<string, unknown>>;
export default class ErrorBoundary {
    private sdkKey;
    private statsigMetadata?;
    private seen;
    constructor(sdkKey: string);
    setStatsigMetadata(statsigMetadata: Record<string, string | number>): void;
    swallow<T>(tag: string, task: () => T): void;
    capture<T>(tag: string, task: () => T, recover: () => T, getExtraData?: ExtraDataExtractor): T;
    logError(tag: string, error: unknown, getExtraData?: ExtraDataExtractor): Promise<void>;
    private onCaught;
    private getDescription;
}
export {};
