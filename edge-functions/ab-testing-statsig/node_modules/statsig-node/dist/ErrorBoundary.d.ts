export declare const ExceptionEndpoint = "https://statsigapi.net/v1/sdk_exception";
export default class ErrorBoundary {
    private sdkKey;
    private statsigMetadata;
    private seen;
    constructor(sdkKey: string);
    swallow<T>(task: () => T): void;
    capture<T>(task: () => T, recover: () => T): T;
    setup(sdkKey: string): void;
    private onCaught;
    logError(error: unknown, key?: string): void;
    private getDescription;
}
