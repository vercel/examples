"use strict";
exports.__esModule = true;
exports.EvaluationDetails = void 0;
var EvaluationDetails = /** @class */ (function () {
    function EvaluationDetails(configSyncTime, initTime, reason) {
        this.configSyncTime = configSyncTime;
        this.initTime = initTime;
        this.reason = reason;
        this.serverTime = Date.now();
    }
    EvaluationDetails.uninitialized = function () {
        return new EvaluationDetails(0, 0, 'Uninitialized');
    };
    EvaluationDetails.make = function (store, reason) {
        return new EvaluationDetails(store.getLastUpdateTime(), store.getInitialUpdateTime(), reason !== null && reason !== void 0 ? reason : store.getInitReason());
    };
    return EvaluationDetails;
}());
exports.EvaluationDetails = EvaluationDetails;
