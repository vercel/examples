/// <reference types="whatwg-fetch" />
import { ExplicitStatsigOptions } from '../StatsigOptions';
export default class StatsigFetcher {
    private sessionID;
    private leakyBucket;
    private pendingTimers;
    private dispatcher;
    private localMode;
    private sdkKey;
    constructor(secretKey: string, options: ExplicitStatsigOptions);
    dispatch(url: string, body: Record<string, unknown>, timeout: number): Promise<Response>;
    post(url: string, body: Record<string, unknown>, retries?: number, backoff?: number): Promise<Response>;
    shutdown(): void;
    private _retry;
}
