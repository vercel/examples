/// <reference types="whatwg-fetch" />
export default class Dispatcher {
    private queue;
    private drainInterval;
    private drainTimer;
    constructor(drainIntervalms?: number);
    enqueue(promise: Promise<Response>, timeoutms: number): Promise<Response>;
    private _scheduleDrain;
    private _drainQueue;
    shutdown(): void;
}
