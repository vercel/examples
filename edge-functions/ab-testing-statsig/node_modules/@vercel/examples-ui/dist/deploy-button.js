import { jsx as _jsx } from "react/jsx-runtime";
import Button from './button.js';
const VERCEL_CLONE = 'https://vercel.com/new/clone';
const DeployButton = (props)=>{
    var ref;
    const params = [
        [
            'repository-url',
            props.repositoryUrl
        ],
        [
            'env',
            (ref = props.env) == null ? void 0 : ref.join(',')
        ],
        [
            'project-name',
            props.projectName
        ],
        [
            'repository-name',
            props.repositoryName
        ], 
    ];
    const query = params.reduce((arr, [k, v])=>{
        if (v) arr.push(`${k}=${encodeURIComponent(v)}`);
        return arr;
    }, []).join('&');
    return /*#__PURE__*/ _jsx(Button, {
        Component: "a",
        href: `${VERCEL_CLONE}${query ? `?${query}` : ''}`,
        target: "_blank",
        rel: "noreferrer",
        children: "Clone & Deploy"
    });
};
export default DeployButton;
