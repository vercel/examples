export default `
Use this tool after you have finalized an app, to ask for human approval
to deploy the app to a preview environment.

Only use this tool after you're done with coding, and only use it once,
unless the user specifically asks you to use it at some specified point.
`
