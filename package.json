{
  "name": "edge-functions",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write .",
    "create-project": "node -r dotenv/config scripts/setup-project.mjs"
  },
  "devDependencies": {
    "prettier": "^2.4.0"
  },
  "dependencies": {
    "node-fetch": "^3.1.1"
  }
}
