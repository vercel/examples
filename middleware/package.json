{
  "name": "edge-functions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "update-next": "node scripts/update-next.js",
    "prettier": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.4.0"
  }
}
