[project]
name = "queues-python-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "pydantic",
  "vercel-cache",
  "vercel-queue",
]

[tool.vercel]
entrypoint = "main:app"

[[tool.vercel.subscribers]]
entrypoint = "subscribers"
